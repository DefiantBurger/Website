# Frontend

This package contains the Svelte + TypeScript + Vite frontend.

## Setup

```bash
cd frontend
pnpm install
pnpm run dev
```

## Notes

- Run the frontend commands from the `frontend/` directory.
